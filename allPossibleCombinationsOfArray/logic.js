let inputArr = ['first', 'second', 'third', 'fourth'];

const generateAllCombinations = require('./generateAllCombinations');
const result = generateAllCombinations(inputArr);
console.dir(result, { depth: null });