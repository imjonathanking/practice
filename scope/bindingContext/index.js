const context = {
    test1: 'Testing',
    test2: 'Really testing'
}

var items = new Array(5).fill(null).map((item, index) => {
    return { id: index, value: 'thingy' }
})



// call and apply
// Remember that .call(), and .apply() actually call your function

